import { PrivateEventEmitter } from 'gs-utils/lib/privateEventEmitter';
import { ModuleWrapper } from './moduleWrapper';
export declare enum CONCAT_STYLE {
    deploy = 1,
    auxiliary = 2,
    entry = 3,
    unit = 4
}
export interface LuaScriptConcatOptions {
    inFile: string;
    concatStyle: CONCAT_STYLE;
    outFile?: string;
    moduleWrapper?: ModuleWrapper;
}
export declare class LuaScriptConcat<C extends LuaScriptConcatOptions = LuaScriptConcatOptions> extends PrivateEventEmitter {
    private inFile;
    private inDir;
    private moduleWrapper;
    private originalScript;
    private outFile;
    private concatStyle;
    private includedFiles;
    private currentScript;
    private finishedScript;
    constructor(config: C, includedFiles?: Set<string>);
    concat(): string;
    finish(entryScript?: string): void;
    private readonly moduleName;
    private reset;
    private includeAuxillaryHeader;
    private includeRequiredFiles;
    private writeUnit;
    private writeDeployment;
    private getIncluded;
    private isInclude;
    private isComment;
    private isIgnoreBegin;
    private isIgnoreEnd;
    private isMainBegin;
    private isMainEnd;
    private isAuxiliaryBegin;
    private isAuxiliaryEnd;
}
export declare function concatDirectory(opts: {
    inDir: string;
    outDir: string;
    type?: CONCAT_STYLE;
    entryScript?: string;
    moduleWrapper?: ModuleWrapper;
}): void;
export declare function createEntryScript(opts: {
    entryTarget: string;
    moduleWrapper?: ModuleWrapper;
}): string;
