export declare class Incrementer {
    private trackIdleTime;
    private trackInterval;
    private map;
    constructor(trackIdleTime: any, trackInterval: any);
    get(item: string): number;
    incr(item: string, value?: number): void;
    clear(item: string): boolean;
    private beginIdleCheck;
    private newValue;
}
export declare const createIncrementer: (idleTime?: number, interval?: number) => Incrementer;
