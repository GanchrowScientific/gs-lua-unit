export declare abstract class Range extends Array {
    private splitter;
    constructor(rawRange: string, splitter?: string);
    protected abstract generateRange(start: number, end: number, step?: number): any;
}
export declare class RangeExclusive extends Range {
    constructor(rawRange: string);
    protected generateRange(start: number, end: number, step?: number): void;
}
export declare class RangeInclusive extends Range {
    protected generateRange(start: number, end: number, step?: number): void;
}
