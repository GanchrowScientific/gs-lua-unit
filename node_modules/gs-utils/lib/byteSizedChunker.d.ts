export declare class ByteSizedChunker {
    private bufferHeaderLength;
    private packFormat;
    private partial;
    constructor(bufferHeaderLength: number, packFormat: string);
    prepare(data: Buffer | string, dataType?: string): Buffer;
    forEachCompleteChunk(dataBuf: Buffer, cb: (string: any) => void): void;
    private getExpectedMessageSize;
    private getMessage;
    private getNextPartial;
}
