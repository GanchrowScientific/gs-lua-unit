export declare class GSThrottle {
    private timeoutId;
    private cycleEndTime;
    private registeredCallback;
    private hardDelay;
    private softDelay;
    constructor(hardDelay: number, softDelay?: number);
    callback(cb: (...args: any[]) => void): void;
    more(...args: any[]): void;
    private wrap;
}
