export declare class Chunker {
    private static LINEFEED;
    private partial;
    private split;
    constructor(split?: string);
    forEachCompleteChunk(dataBuf: Buffer | string, cb: (string: any) => void): void;
}
