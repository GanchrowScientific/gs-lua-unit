export declare class Backoff {
    private registeredCBs;
    private registeredEnds;
    private timeout;
    private retryCount;
    private retries;
    private baseInterval;
    private truncate;
    private state;
    constructor(backoffOpts?: {});
    register(cb: (...args: any[]) => void): void;
    onEnd(cb: () => void): void;
    fire(...args: any[]): void;
    reset(): void;
    private calculateTime;
}
