import 'source-map-support/register';
import * as Rx from 'rxjs';
export declare class FileSlurper {
    private filePath;
    private split?;
    private chunker;
    private isBinary;
    constructor(filePath: string, split?: string);
    slurp(): Rx.Observable<string>;
    private createStream;
}
